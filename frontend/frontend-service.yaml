apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: gruppe12
  labels:
    app: frontend
spec:
  type: ClusterIP
  ports:
    - port: 8888
      targetPort: 8888
      protocol: TCP
  selector:
    app: frontend
