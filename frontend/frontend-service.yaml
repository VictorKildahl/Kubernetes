apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: gruppe12
  labels:
    app: frontend
spec:
  type: LoadBalancer
  ports:
    - port: 8888
      targetPort: 80
      protocol: TCP
  selector:
    app: frontend
